static mmio($FF40) LCDC: u8;

static mmio($FF44) LY: u8;

const VBLANK_START: u8 = 144;

fn main() {
  loop {
    if *LY == VBLANK_START {
      break
    }
  }

  *LCDC = 0;

  let a = 1;
  loop {}
}
