
// Audio Master Control
const NR52 = $FF26

// LCD Control
const LCDC = $FF40

// LCD Y coordinate (aka "current scanline")
const LY = $FF44

fn main() {

  // shut down the audio
  a = 0
  //[NR52] = a

  // only turn off the LCD during vblank
  loop {
    //a = [LY]
    //if a >= 144 {
    //  break
    //}
  }
  a = 0
  //[LCDC] = a
}
