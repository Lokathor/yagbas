
fn main() {
  [NR52] = AudioMain {}
  'wait_for_vblank: loop {
    if [LY] == VBLANK_START {
      break
    }
  }
  [LCDC] = LcdCtrl {}
  
  memcpy($9000, Tiles, size_of!(Tiles))
  memcpy($9800, Tilemap, size_of!(TileMap))
  
  [LCD] = LcdCtrl { lcd_enabled, bg_win_enabled }
  [BGP] = STANDARD_PALETTE
}

static Tiles = todo

static TileMap = todo
