// sample file for control flow graph creation testing.

// this file should remain as one function with no named const values

fn main() {
  a = $10
  loop {
    [$ff00] = a
    if a != $1F {
      break
    } else {
      b = 1
    }
  }
  a = 0
}
